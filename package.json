{
  "devDependencies": {
    "cypress": "^9.7.0",
    "cypress-xpath": "^2.0.1"
  },
"scripts": {
    "cy:run": "cypress run",
    "cy:chrome":"cypress run --browser chrome",
    "cy:edge":"cypress run --browser edge"
  }

 
}